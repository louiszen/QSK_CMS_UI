apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubenetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    kubernetes.io/ingress.class: internal
  name: gptw-web-dev-ingress-internal
  namespace: aat-test
  labels:
    app: gptw-web-dev
    name: gptw-web-dev
    kind: ingress
    tier: web
    purpose: dev
    ingress: internal
spec:
  rules:
  - host: "gptw-web-poc.dev.gammonconstruction.com"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
         service:
           name: gptw-web-dev-svc
           port: 
            number: 80