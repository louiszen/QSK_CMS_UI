apiVersion: v1
kind: Service
metadata:
  name: gptw-web-dev-svc
  labels:
    app: gptwwebdev
    name: gptw-web-dev
    kind: service
    tier: web
    purpose: dev
spec:
  selector:
    app: gptwwebdev
    name: gptw-web-dev
    kind: deployment
    tier: web
    purpose: dev
  ports:
  - port: 80
    targetPort: 80